var express = require('express');
var router = express.Router();

var auth = require('../handlers/auth');

//router.get('/new', auth.new);
//router.get('/parse', auth.parse);

module.exports = router;
